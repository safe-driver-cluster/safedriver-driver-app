name: safe_driver_driver_app
description: "SafeDriver driver mobile application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5
  cloud_functions: ^4.7.6
  firebase_storage: ^11.7.7
  image_picker: ^1.0.4
  geolocator: ^10.1.0
  shared_preferences: ^2.2.2
  intl: ^0.20.0
  country_code_picker: ^3.4.1
  google_maps_flutter: 2.10.1
  google_maps_flutter_android: ^2.19.8
  google_maps_flutter_platform_interface: ^2.13.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
